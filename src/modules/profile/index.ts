/**
 * API pública del módulo profile — cliente y servidor.
 * Los reads server-only viven en `./server`.
 */
export { updateMyProfileAction } from "./actions";
export { ProfileForm } from "./components";
export type { ProfileDto } from "./types";
